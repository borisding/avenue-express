import { path as root } from 'app-root-path';

export default {
  root,
  bin: `${root}/bin`,
  config: `${root}/config`,
  dist: `${root}/dist`,
  src: `${root}/src`,
  logs: `${root}/logs`,
  views: `${root}/src/app/views`
};
